# What 1.0.0 has to cover

Every axiom, every idea in the old repo's 149 commits, and every open research
thread, sorted into four bins. The first is done. The second is what stands
between the tree and a 1.0.0 tag. The third is 1.0.1 by written decision. The
fourth is research with the experiment that would settle it.

Sources: `gorkbot/.wiki/axioms.md`, the old repo's `docs/2026-09-01-axiom-backlog.md`,
`docs/seam-register.md`, `TODO.md`, `impl/BRIEF.md`, and its full commit history.
The old repo is a quarry: much of it is worth learning from, none of it is trusted
by default.

## 1. Covered

| axiom / idea | where |
|---|---|
| 0, one place to stand | `main.py`: reception is the only door |
| 1, one voice, a staff, a door to each | `message` tool, `@name` transfer, no hierarchy |
| 2, staff by aptitude | `bots.json` maps names to roles; cast ranks per role |
| 2 corollary, many kernels per task | `arity N "text"` |
| 3, provider / model / effort weighed per prompt | `cast.choose`: aptitude orders, quota filters; `Spec.effort` paired with the model |
| 3, standings pairwise with counts, rate not running total | `scorecard.standings(role, factor)` |
| 3, exploration slot | `trial.candidates`: the last slot goes to the least-tried model |
| 3, ruleset epoch | `library/EPOCH`, stamped on every birth line, only current rows vote |
| 3, requested versus satisfied, never padded | `main.run_trial` says when fewer models have quota |
| 5, mine over rented, stated as policy | `seams.py` |
| 7, a seat is sticky for a kernel's life | one `State` per bot in `loop.live` |
| 7, spend the seat closest to reset first | `seats.with_quota`, `trial.candidates` |
| 7, keep the cached prefix warm while the person is still there | `Seat.warm_window`, `Loop.keep_warm`, the `Warmer` thread |
| 9, two accounts, archivist as fallback | `ledger.death_rites` |
| 12, elegant core, plugs behind seams | five Protocols, pure `transition`, `wire_mock` |
| 12, home turf harnesses | `harness.py`, flags verified on this machine |
| 12, the quiet-failure rule, written down | `seams.py` |
| 13, male join demonstrable | four interchangeable Model plugs |
| presence, one live kernel per bot across processes | `loop.claim`, a lock file per bot |
| the pure moment with injectable ids | `moment.py` |
| the journal as the state; resume is a fold | `store.py`, `Loop.resume` |
| lineage: birth says who woke you, the journal says who spoke to you after | birth line, Message events |
| usage per fork | `store.usage`, `Result.usage` |
| the flower and `--arity N` | README, `main.py` |

## 2. Before the tag

- One real run of each new path: a trial from the front door with a pick, a resume
  after a killed process, a keepalive firing on a subscription seat.
- Ideate on task kind for an hour. Role is the grouping for 1.0.0 unless that
  hour says otherwise; the store already keeps a one-line summary per task so a
  finer taxonomy can grow later.
- Fold the old repo's SECURITY.md across. Version to 1.0.0. Tag. Swap the folders. Push.

## 3. Deferred to 1.0.1 by decision

Decided in conversation on 2026-09-02 unless marked otherwise.

| item | decision |
|---|---|
| Pulse (axiom 11) | Out. `Tick` removed. A keepalive timer replaces it. The wiki's axiom 11 should be struck or rewritten as "keep the cache warm". |
| Memory tier (axiom 8) | Good idea, downstream of a cleaner way of assembling a kernel and all its levers. One wake line for now. The wiki should mark the axiom deferred so the two documents agree. |
| The word "kernel" | Settled: a kernel is one model runtime, as the wiki defines it. The core of arity is the moment and the loop, and nothing else is called a kernel. |
| Task kind | The role, for 1.0.0. Revisit before the tag. |
| Type packs | Folded into roles plus skills. |
| Scorecard judge, discrepancy penalties, honesty axes | The box takes a human pick. Ground on later with data. |
| Subscription OAuth (`auth.py`, ~1,000 lines) | Not ported. A pinned CLI harness carries its own login; verified for Claude Max. |
| LiteLLM, real MCP, Docker or any sandbox | Plugs behind seams that do not move. |
| TUI, phone (axiom 6), red phone public address (axiom 10) | Surfaces on Transport. When the TUI comes, the flower is its loading screen. |
| Denial-set roles, OS-level confinement | Text only for now; confinement is the sandbox plug's job. |
| Frozen evidence bundles, `Resolution` | The journal is the evidence; a fork's file replays on its own. |
| Casting modes (smart / brokie / chaos) and a recorded seed | After there is data to distinguish them. Chaos needs a seed, which needs a mode. |
| Web search and fetch tools, task bank with hidden tests | Library content, not core. |
| Metrics observer, inspection CLI, SQLite store | `Observer` and `Store` seams exist; plugs later. |

Open for Asa, not blocking: the old code cites "axiom 36" (never cast a seat a
person is live on) and the wiki has no such axiom. Register it or drop the number.

Pointers into the old repo for when these come back:

- A complete TUI observer with ~3,300 lines of tests sits on an unmerged branch,
  tip `1bcb534`. The old TODO still lists it as unbuilt. Start there.
- `arity/auth.py` on `main`: PKCE and device-code login for four providers, atomic
  credential writes, discovery of `~/.codex/auth.json` and the omp SQLite store.
- `arity/evidence.py`, `trial_events.py`: the bundle and replay design.
- `arity/archivist.py`: the discrepancy multiplier and honesty axes.
- Tried and abandoned there, do not retry blind: running-total standings;
  weighted-sum casting with coefficients; versioning the signature key; routing
  swallowed exceptions through the observer; an OMP model provider.

## 4. Research, with the experiment that settles each

- Does an OpenAI cache read restart its 30-minute window? Same prefix at t=0, 25, 55 min; compare `cached_tokens` on the third call.
- Do two keys in one OpenAI project share a cache? Cold on key 1, immediately key 2; compare `cached_tokens`.
- Does a Gemini implicit-cache read restart its window? Same spacing; compare `cachedContentTokenCount`.
- xAI minimum cacheable prefix, and whether keys in one team share. Grow the prefix until `cached_tokens` appears.
- Does the keepalive actually register as a cache hit at each provider? Ping, then a real call; compare the real call's cached-token count with and without the ping.
- Can an Anthropic OAuth token from the old `auth.py` hit the Messages API with a bearer header? One raw POST.
- Does `codex exec` put only the final answer on stdout on a run with quota? Rerun the stdin test when quota resets.
- Where do cache breakpoints go, and what does a trial that varies the tool schema cost versus one that varies the runner? `wire_anthropic.py` is the only file that would ever write them.
- The closed loop (A3-5): discover models, A/B them on real tasks, retrain the policy with no operator. Nobody has this. It is the part that is ours.

## Reading the count

Every numbered axiom is now embodied, deferred by a written decision, or named
as needing an amendment in the wiki. That is what "complete" means here.
