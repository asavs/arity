{
  "hands": "read_file + write_file work headless via: register the folder as a project with --new-project, then run with --mode accept-edits (a later run can omit --new-project and reuse the registered project). shell (\"command\" permission) does NOT work headless for arbitrary commands: it is hard-denied unless the exact command string is already present in permissions.allow inside the global ~/.gemini/antigravity-cli/settings.json (verified: `git diff`, the one pre-existing entry there, ran successfully; `git status`, present only in the separate shared ~/.gemini/config/config.json 13-entry list, was still denied). No project-local/in-directory settings file exists anywhere for agy to grant shell permission, and no CLI subcommand/flag grants it either.",
  "harness": "agy.exe --output-format json --print-timeout 3m [--new-project first run only] --mode accept-edits --print='<prompt>' (run from inside the target directory). --sandbox adds restrictions, does not help. -i/--prompt-interactive with piped stdin hangs indefinitely (needs a real TTY) and is not viable headless.",
  "model": "gemini-3.7-flash",
  "effort": "high",
  "saw_ground_truth": false,
  "wrote_outside_dir": false,
  "note": "Real BRIEF.md run was NOT executed. write+read are legitimately unblockable headless (via --new-project + --mode accept-edits, no global-settings edits), but shell is not: the only two unlocks the CLI itself documents are adding an allow-rule to the global ~/.gemini/antigravity-cli/settings.json or --dangerously-skip-permissions, both excluded by the task's hard limits. Running the brief without working shell would reproduce the exact failure mode (fake tool success / silent no-op on `python demo.py`) this investigation was meant to avoid, so it was skipped per instructions and reported as a blocker instead. Probe artifacts (probe_a..d2.json/.err, hello.txt from probe C) are left in this directory as evidence; no files were written outside C:\\Users\\example\\Projects\\arity\\impl\\agy3."
}
